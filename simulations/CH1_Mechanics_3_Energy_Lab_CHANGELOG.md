# CH1_Mechanics_3_Energy_Lab — changelog

**Next release: `2.1.0`** — set by the user on 2026-09-03. The next round of
edits bumps the minor number, not the patch; patch counting resumes from
2.1.1 after that.

**Versioning rule.** `major.minor` change only when the lab is rebuilt from a new
prompt file. The `patch` number counts the edit rounds since that build — one
bump per round of changes that is delivered and written to disk. The file name
never changes; the same file is always overwritten.

| version | round | what changed |
|---|---|---|
| 2.0.0 | — | built from `CH1_Mechanics_3_Energy_Lab_Prompt_EN_v2.md` |
| 2.0.1 | 1 | Sankey diagram redrawn to textbook proportions, self-sizing card, note card removed |
| 2.0.2 | 2 | chemical reaction as ball-and-stick molecules (CPK colours) |
| 2.0.3 | 3 | reaction animation reworked: atoms slide to new partners instead of scattering |
| 2.0.4 | 4 | Mode 3 caption moved clear of the animation; "Friction" → "Dissipation" (per-process wording) |
| 2.0.5 | 5 | one-run playback with ↻ Replay; fixed-width Play/Pause; Mode 1 caption strips moved top-left; heater added to the thermal scene; sound-wave direction |
| 2.0.6 | 6 | wave marker removed; equation/joules chips swapped corners; Mode 1 formula + note merged into one card |
| 2.0.7 | 7 | GPE gets its own graph; helical heating element; tuning fork redrawn as one piece |
| 2.0.8 | 8 | fork tips aligned with the sound axis; friction scene rebuilt around an initial speed |
| 2.0.9 | 9 | velocity vector moved above the box; lifts kept clear of the header; impulse-shaped F(x); F–s graph became a draggable card with a fitted heading; left margins; braking-friction direction; Efficiency folded into the Sankey scene; **version stamping moved into `VERSION` + this changelog** |
